const capitalize = str =>{

}

const reverseString = str =>{

}

const calculator = {
    add : (x,y)=>{
        return x + y;
    },

    subtract : (x,y)=>{
        return x - y;
    },

    multiply : (x,y)=>{
        return x * y;
    },

    divide : (x,y)=>{
        return x / y; 
    }

};

const caesarCipher = (str) =>{
    
}

export {
    capitalize,
    reverseString,
    calculator,
    caesarCipher
}